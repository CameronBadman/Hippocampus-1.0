# Phase B artifacts

This directory receives the renderer causal-test JSONL ledger and generated
aggregate reports. Raw run directories, logs, and checkpoints are deliberately
ignored by Git; every selected checkpoint receives a content-addressed
manifest, and final remote replication artifacts will be copied to durable
storage after a finalist is frozen.
